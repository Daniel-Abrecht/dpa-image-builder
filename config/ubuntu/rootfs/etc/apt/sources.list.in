deb     $CHROOT_REPO $RELEASE           $S_COMPONENTS
deb-src $CHROOT_REPO $RELEASE           $S_COMPONENTS
#deb     $CHROOT_REPO $RELEASE-updates   $S_COMPONENTS
#deb-src $CHROOT_REPO $RELEASE-updates   $S_COMPONENTS
deb     $CHROOT_REPO $RELEASE-security  $S_COMPONENTS
deb-src $CHROOT_REPO $RELEASE-security  $S_COMPONENTS
#deb     $CHROOT_REPO $RELEASE-backports $S_COMPONENTS
#deb-src $CHROOT_REPO $RELEASE-backports $S_COMPONENTS
